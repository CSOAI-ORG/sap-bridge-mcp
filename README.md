# sap-bridge-mcp

mcp-name: io.github.CSOAI-ORG/sap-bridge-mcp

Part of the **CSOAI Layer-0 legacy-bridge family** (sibling of `cobol-bridge-mcp`). Bridge SAP IDoc / RFC / ABAP legacy to ONE OS — parse, map, govern (SAP GRC / SOX).

**Tools:** parse_idoc · validate_idoc · map_to_modern · govern_sap

```bash
pip install -e .
python server.py
```
